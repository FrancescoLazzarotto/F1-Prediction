# No API keys are required — all data sources are free and open.
# Add optional overrides below if needed.

# Cache directory override (defaults to .cache/ in project root)
# F1PREDICT_CACHE_DIR=.cache
